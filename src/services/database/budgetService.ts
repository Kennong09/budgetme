import { supabase } from '../../utils/supabaseClient';

// TODO: Implement with Supabase client instead of Prisma
// This service is currently not used in the frontend
export class BudgetService {
  // Service methods will be implemented when needed
}
